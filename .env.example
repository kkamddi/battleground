# Keep real credentials outside Git.
# Vercel authentication should use a local CLI session or a local VERCEL_TOKEN.
VERCEL_TOKEN=

# Server-side data jobs only. Never prefix the service-role key with NEXT_PUBLIC_.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
PUBG_API_KEY=
